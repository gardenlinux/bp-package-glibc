version=2.41-12
version_suffix=gl0~bp1877
apt_src glibc
salsasrc=$(mktemp -d)
git clone --depth 1 -b debian/$version https://salsa.debian.org/glibc-team/glibc.git $salsasrc
rm -r $dir/src/debian
mv $salsasrc/debian $dir/src/
rm -r $salsasrc
